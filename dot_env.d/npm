if command -v npm &>/dev/null && [ -z "${NPM_CONFIG_PREFIX}" ]; then
  export NPM_CONFIG_PREFIX="${HOME}/.npm"
  LOCAL_PATH="${NPM_CONFIG_PREFIX}/bin"
  if ! [[ "$PATH" =~ "${LOCAL_PATH}" ]]; then
    export PATH="${LOCAL_PATH}:${PATH}"
  fi
fi
